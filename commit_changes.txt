Commit no(13):
    NEW: mini_client/client.py
        Needed for testing NameNode allocation and commit functionality
        test_namenode_methods(host='localhost', port=50051)
            use case: Emulates file writing by requesting block allocation and committing file
    
    NEW: mini_client/delete_client.py
        Needed for testing NameNode delete functionality
        delete_file(host='10.10.20.60', port=50051, file_name='test_file.txt')
            use case: Emulates file deletion by sending delete request to NameNode
    
    namenode/app/allocation.py
        get_available_nodes(self, block_size)
            use case: Finds available DataNodes with sufficient space and active status
        allocate(self, file_details, no_of_stripes, no_of_shards, block_size)
            use case: Allocates blocks across DataNodes for file storage using round-robin
        commit_block(self, file_name, block_id, cur)
            use case: Commits block allocation to database and releases node assignment
        release_nodes(self, blocks: List[str])
            use case: Rolls back block allocations and frees up DataNode assignments
    
    namenode/app/main.py
        main(flag: int=0)
            use case: Modified main function to accept flag for running in test mode or prod mode
    
    namenode/app/server.py
        CommitFile(self, request, context)
            use case: Commits file metadata to database with error handling and rollback
        DeleteFile(self, request, context)
            use case: Deletes file and metadata from database with proper cleanup
    
    
    namenode/db_manager/check_tables.py
        check_tables()
            use case: Enhanced to display all three tables (dn_table, file_table, metadata)
    
    NEW: namenode/db_manager/fakenode.py
        Needed for inserting test DataNodes for development
        insert_fake_datanodes()
            use case: Creates 10 fake DataNode entries in dn_table for testing
    
    namenode/db_manager/init-db.py
        main()
            use case: Modified to initialize all three SQL tables (dn_table, file_table, metadata)
    
    NEW: namenode/db_manager/nuke.py
        Needed for clearing database during development
        clear_tables()
            use case: Clears all data from dn_table, file_table, and metadata tables
    
    namenode/models/metadata.sql
        Updated id field from AUTOINCREMENT to AUTO_INCREMENT
            use case: Fixed MySQL syntax for auto-increment primary key

Commit no(14):
    NEW: mini_client/read_client.py
        Needed for testing NameNode GetFileMetadata functionality
        get_file_metadata(host='192.168.1.166', port=50051, file_name='test_file.txt')
            use case: Retrieves and displays file metadata including block groups and placements
    
    namenode/app/allocation.py
        __init__(self, registry:DataNodeRegistry, logger:Logger, data_blocks:int=6, parity_blocks:int=3)
            use case: Enhanced constructor to support configurable data and parity block policies
        set_policy(self, data_blocks:int, parity_blocks:int)
            use case: Dynamically updates allocation policy for different erasure coding schemes
    
    namenode/app/registry.py
        load_state(self)
            use case: Modified to set DataNode status to "ACTIVE" for testing purposes
    
    namenode/app/server.py
        AllocateBlocks(self, request, context)
            use case: Enhanced to set allocation policy dynamically based on request parameters
        ListFiles(self, request, context)
            use case: Enhanced to log file details from database for debugging
        GetFileMetadata(self, request, context)
            use case: Implements complete metadata retrieval with proper block grouping and stripe formation
    Note: It is known bug that first read from namenode will bug out due to the fact that EC policy is volatile. 
          This will be fixed in next commits

Commit no(15):
    namenode/models/metadata.sql
        Renamed table from 'metadata' to 'metadata_table'
            use case: Fixed table name consistency across all database operations
        
    namenode/models/filetable.sql
        Added data_blocks and parity_blocks columns
            use case: Store erasure coding policy parameters per file for consistent metadata retrieval
    
    namenode/app/server.py
        Fixed all SQL queries to use 'metadata_table' instead of 'metadata'
            use case: Ensured database operations reference correct table name
        DeleteFile method improvements:
            - Added proper block retrieval before deletion
            - Fixed AUTO_INCREMENT reset with proper SQL formatting
            - Enhanced error handling with rollback
            use case: Ensured complete file deletion with proper database cleanup
        CommitFile method:
            - Added data_blocks and parity_blocks to file_table insert
            use case: Store erasure coding policy for each committed file
        GetFileMetadata method:
            - Retrieve data_blocks and parity_blocks from file_table
            - Use allocation_manager.send_metadata for block grouping
            use case: Consistent metadata retrieval using stored policy parameters
    
    namenode/app/allocation.py
        delete_blocks method signature change:
            - Changed from delete_blocks(self, file_name, cur) to delete_blocks(self, blocks, cur)
            use case: Pass pre-fetched blocks for better performance and consistency
        Added send_metadata method:
            - Centralized block grouping logic from server
            use case: Reusable metadata formatting for both allocation and retrieval
        Enhanced commit_block:
            - Added size field to metadata insertion
            use case: Store block size for proper space accounting during deletion
    
    namenode/app/registry.py
        load_state method:
            - Set initial DataNode status to "INACTIVE" instead of "ACTIVE"
            use case: Ensure DataNodes start inactive until heartbeat received
    
    namenode/db_manager/check_tables.py
        Updated to query 'metadata_table' instead of 'metadata'
            use case: Consistent database table verification
    
    mini_client files:
        Updated default file names from 'test_file.txt' to 'text_file.txt'
        Updated erasure coding parameters (data_blocks=6, parity_blocks=3)
            use case: Consistent testing parameters across all client scripts